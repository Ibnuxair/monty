#include "monty.h"
/**
 * pall_file - prints the stack
 * @head: This is the head of the stack
 * @counter: This will not be used here
 * Return: always nothing
*/
void pall_file(stack_t **root, unsigned int line_number)
{
	stack_t *tmp;

	(void)counter;
	tmp = *head;
	if (tmp == NULL)
		return;
	while (tmp)
	{
		printf("%d\n", h->n);
		h = h->next;
	}
}
